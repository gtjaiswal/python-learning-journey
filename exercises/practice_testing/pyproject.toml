[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "practice_testing"
version = "0.1.0"
description = "Python testing practice exercises"
requires-python = ">=3.8"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]